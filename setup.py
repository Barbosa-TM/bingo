from setuptools import setup

setup(name='bingo',
      description='lotto numbers generator and caller',
      author='tm-barbosa',
      license='MIT',
      install_requires= ["gtts",
                         "cprint",
                         "pyfiglet",
                         "python-vlc"],
      zip_safe=False)
